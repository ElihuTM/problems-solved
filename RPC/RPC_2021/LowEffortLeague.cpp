#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll memo[ maxn ][ maxn ];

ll dp( int round, int jugador )
{
	for (int i = 0; i < (1<<n); ++i)
	{
		if ()
		{
			/* code */
		}
	}
}

int main()
{
	int n;
	cin >> n;
	for (int i = 0; i < (1<<n); ++i)
		cin >> arr[ i ];
	
	for (int i = 0; i < ; ++i)
	{
		/* code */
	}
	
	return 0;
}