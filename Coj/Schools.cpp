#include <bits/stdc++.h>

using namespace std;
typedef vector<int> vi;
typedef vector< vi > vvi;

int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	vii cost;
	int n, ni,a,b,c;
	cin >> n;

	cost

	for (int i = 0; i < n; ++i)
	{
		cin >> ni >> a >> b >> c;

	}
}