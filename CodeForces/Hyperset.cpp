#include <bits/stdc++.h>

using namespace std;
typedef vector<string> vs;

int main()
{
	int n, k;

	cin >> n >> k;
	vs cad( n );

	for (int i = 0; i < n; ++i)
		cin >> cad[ i ];

	sort( cad.begin(), cad.end() );

	for (int i = 0; i < n; ++i)
	{
		
	}
	return 0;
}